const Footer = () => {
  return (
    <footer>
      {/* {data.map((task: any) => {
        console.log(task);
        return (
          <li key={task.id} className="bg-indigo-200">
            {task.text}
          </li>
        );
      })} */}
    </footer>
  );
};

export default Footer;
